# Customer Service Chatbot Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add an AI-powered customer service chat widget to the GameOn landing page that answers visitor questions about features, pricing, and getting started.

**Architecture:** A floating circular button (bottom-right) opens a chat panel. The panel POSTs conversation history to `/api/chat`, which calls the Anthropic API with a GameOn system prompt and returns a reply. All state is client-side only — no server persistence.

**Tech Stack:** Next.js 14 App Router, `@anthropic-ai/sdk`, React hooks, TypeScript, Vercel (deployment)

---

## File Map

| File | Action | Responsibility |
|---|---|---|
| `components/ChatWidget.tsx` | Create | Floating button + chat panel UI, message state, API calls |
| `app/api/chat/route.ts` | Create | Calls Anthropic API with GameOn system prompt |
| `app/page.tsx` | Modify (last line) | Import + render `<ChatWidget />` |

---

### Task 1: Install Anthropic SDK

**Files:**
- Modify: `package.json` (via npm install)

- [ ] **Step 1: Install the SDK**

```bash
cd /Users/signevallin/Desktop/GameOn
npm install @anthropic-ai/sdk
```

Expected output: `added 1 package` (or similar — no errors)

- [ ] **Step 2: Verify it's in package.json**

```bash
grep anthropic package.json
```

Expected: `"@anthropic-ai/sdk": "^0.x.x"`

- [ ] **Step 3: Commit**

```bash
git add package.json package-lock.json
git commit -m "chore: install @anthropic-ai/sdk"
```

---

### Task 2: Create the chat API route

**Files:**
- Create: `app/api/chat/route.ts`

This is a public POST route (no auth). It receives `{ messages: { role, content }[] }`, prepends a GameOn system prompt, calls `claude-3-5-haiku-20241022`, and returns `{ reply: string }`.

- [ ] **Step 1: Verify the route doesn't exist yet**

```bash
ls /Users/signevallin/Desktop/GameOn/app/api/chat 2>/dev/null || echo "not found"
```

Expected: `not found`

- [ ] **Step 2: Create `app/api/chat/route.ts`**

```typescript
import { NextRequest, NextResponse } from 'next/server';
import Anthropic from '@anthropic-ai/sdk';

export const dynamic = 'force-dynamic';

const SYSTEM_PROMPT = `You are a friendly and concise customer support assistant for GameOn (playgameon.app).

GameOn is a web-based platform for creating and running team-based competitions and games at events. No app download required — players join instantly via a game key in their browser.

PLANS AND PRICING:
- Starter (free): 1 active game at a time, up to 5 teams, core missions (multiple choice, text answer)
- Pro (499 SEK/year, launch pricing): unlimited games, up to 20 teams, custom missions, photo missions, Power-Ups, PDF reports after each game
- Studio (999 SEK/year, launch pricing): unlimited games, unlimited teams, all Pro features, priority support
- All paid plans are billed annually. Payments handled by Stripe.

HOW IT WORKS FOR ORGANIZERS:
1. Create a free account at playgameon.app
2. Create a game and add missions
3. Share the game key with players
4. Start the game and watch the live leaderboard
5. Download a PDF report when the game ends

HOW IT WORKS FOR PLAYERS:
1. Go to playgameon.app (no account needed)
2. Enter the game key provided by the organizer
3. Enter your team name
4. Play missions and earn points

MISSION TYPES:
- Multiple choice: teams pick the correct answer
- Text answer: teams type a free-text answer
- Photo mission: teams take or upload a photo as their answer
- Scavenger hunt: teams find and photograph specific items

POWER-UPS (Pro and Studio only):
Special strategic moves teams can activate during a game to gain advantages — such as stealing points from another team, doubling their own points on a mission, or blocking opponents. This is a unique GameOn feature not found in other platforms.

OUT OF SCOPE — for these topics, always direct the user to hello@playgameon.app:
- Account-specific issues (login problems, can't access account)
- Billing disputes or refund requests
- Bug reports
- Anything requiring access to a specific user account or game

TONE: Friendly, helpful, and concise. Answer in 1-3 sentences when possible. Never make up features or prices not listed above. If you are not sure, say so and suggest emailing hello@playgameon.app.`;

type Message = { role: 'user' | 'assistant'; content: string };

export async function POST(req: NextRequest) {
  try {
    const { messages }: { messages: Message[] } = await req.json();

    if (!Array.isArray(messages) || messages.length === 0) {
      return NextResponse.json({ error: 'messages required' }, { status: 400 });
    }

    const client = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY });

    const response = await client.messages.create({
      model: 'claude-3-5-haiku-20241022',
      max_tokens: 512,
      system: SYSTEM_PROMPT,
      messages: messages.slice(-10), // last 10 messages to keep cost low
    });

    const reply = response.content[0].type === 'text' ? response.content[0].text : '';
    return NextResponse.json({ reply });
  } catch (err) {
    console.error('[chat]', err);
    return NextResponse.json({ error: 'internal error' }, { status: 500 });
  }
}
```

- [ ] **Step 3: Verify the file was created**

```bash
ls app/api/chat/route.ts
```

Expected: `app/api/chat/route.ts`

- [ ] **Step 4: Commit**

```bash
git add app/api/chat/route.ts
git commit -m "feat: add /api/chat route for AI chatbot"
```

---

### Task 3: Create the ChatWidget component

**Files:**
- Create: `components/ChatWidget.tsx`

A self-contained React component with no props. Manages all state internally. Matches GameOn's design system: background `#0D1520`/`#131F2E`, accent `#7CBDD4`, font Sora.

- [ ] **Step 1: Create `components/ChatWidget.tsx`**

```tsx
'use client';
import { useState, useRef, useEffect } from 'react';

type Message = { role: 'user' | 'assistant'; content: string };

const WELCOME: Message = {
  role: 'assistant',
  content: "Hi! I can answer questions about GameOn — features, pricing, or how to get started. What would you like to know?",
};

export default function ChatWidget() {
  const [open, setOpen] = useState(false);
  const [messages, setMessages] = useState<Message[]>([WELCOME]);
  const [input, setInput] = useState('');
  const [loading, setLoading] = useState(false);
  const bottomRef = useRef<HTMLDivElement>(null);

  // Auto-scroll to latest message
  useEffect(() => {
    if (open) bottomRef.current?.scrollIntoView({ behavior: 'smooth' });
  }, [messages, open, loading]);

  async function send() {
    const text = input.trim();
    if (!text || loading) return;

    const userMsg: Message = { role: 'user', content: text };
    const next = [...messages, userMsg];
    setMessages(next);
    setInput('');
    setLoading(true);

    try {
      const res = await fetch('/api/chat', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ messages: next }),
      });
      const data = await res.json();
      const reply = data.reply || "Sorry, something went wrong. Email us at hello@playgameon.app";
      setMessages(m => [...m, { role: 'assistant', content: reply }]);
    } catch {
      setMessages(m => [...m, { role: 'assistant', content: "Sorry, something went wrong. Email us at hello@playgameon.app" }]);
    } finally {
      setLoading(false);
    }
  }

  return (
    <>
      {/* Chat panel */}
      {open && (
        <div style={{
          position: 'fixed', bottom: 88, right: 24, width: 340, height: 480,
          background: '#131F2E', border: '1px solid rgba(124,189,212,0.15)',
          borderRadius: 16, display: 'flex', flexDirection: 'column',
          boxShadow: '0 20px 60px rgba(0,0,0,0.6)', zIndex: 9999,
          fontFamily: "'Sora', sans-serif",
        }}>
          {/* Header */}
          <div style={{
            background: 'linear-gradient(135deg,#7CBDD4,#4890aa)',
            padding: '12px 16px', borderRadius: '16px 16px 0 0',
            display: 'flex', alignItems: 'center', justifyContent: 'space-between',
          }}>
            <div style={{ display: 'flex', alignItems: 'center', gap: 10 }}>
              <div style={{
                width: 32, height: 32, borderRadius: '50%',
                background: 'rgba(13,21,32,0.3)',
                display: 'flex', alignItems: 'center', justifyContent: 'center',
              }}>
                <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="#DCE4EE" strokeWidth="2.2" strokeLinecap="round" strokeLinejoin="round">
                  <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/>
                </svg>
              </div>
              <div>
                <div style={{ color: '#0D1520', fontWeight: 700, fontSize: 13 }}>GameOn Support</div>
                <div style={{ color: 'rgba(13,21,32,0.6)', fontSize: 11 }}>Powered by AI</div>
              </div>
            </div>
            <button
              onClick={() => setOpen(false)}
              style={{ background: 'none', border: 'none', color: '#0D1520', fontSize: 20, cursor: 'pointer', opacity: 0.6, lineHeight: 1, padding: '0 4px' }}
              aria-label="Close chat"
            >×</button>
          </div>

          {/* Messages */}
          <div style={{ flex: 1, overflowY: 'auto', padding: '14px 14px 8px', display: 'flex', flexDirection: 'column', gap: 10 }}>
            {messages.map((msg, i) => (
              <div key={i} style={{ display: 'flex', justifyContent: msg.role === 'user' ? 'flex-end' : 'flex-start', gap: 8, alignItems: 'flex-start' }}>
                {msg.role === 'assistant' && (
                  <div style={{
                    width: 24, height: 24, borderRadius: '50%', flexShrink: 0, marginTop: 2,
                    background: 'linear-gradient(135deg,#7CBDD4,#4890aa)',
                  }} />
                )}
                <div style={{
                  maxWidth: 230,
                  background: msg.role === 'assistant' ? 'rgba(255,255,255,0.06)' : 'rgba(124,189,212,0.15)',
                  border: msg.role === 'user' ? '1px solid rgba(124,189,212,0.2)' : 'none',
                  borderRadius: msg.role === 'assistant' ? '4px 12px 12px 12px' : '12px 4px 12px 12px',
                  padding: '9px 12px',
                }}>
                  <p style={{ color: '#DCE4EE', fontSize: 13, lineHeight: 1.6, margin: 0 }}>{msg.content}</p>
                </div>
              </div>
            ))}

            {/* Typing indicator */}
            {loading && (
              <div style={{ display: 'flex', gap: 8, alignItems: 'flex-start' }}>
                <div style={{ width: 24, height: 24, borderRadius: '50%', flexShrink: 0, marginTop: 2, background: 'linear-gradient(135deg,#7CBDD4,#4890aa)' }} />
                <div style={{ background: 'rgba(255,255,255,0.06)', borderRadius: '4px 12px 12px 12px', padding: '12px 14px', display: 'flex', gap: 4, alignItems: 'center' }}>
                  {[0, 1, 2].map(i => (
                    <div key={i} style={{
                      width: 6, height: 6, borderRadius: '50%', background: '#7CBDD4',
                      animation: 'chat-dot 1.2s ease-in-out infinite',
                      animationDelay: `${i * 0.2}s`,
                    }} />
                  ))}
                </div>
              </div>
            )}
            <div ref={bottomRef} />
          </div>

          {/* Input */}
          <div style={{ padding: '10px 12px 12px', borderTop: '1px solid rgba(124,189,212,0.1)', display: 'flex', gap: 8 }}>
            <input
              value={input}
              onChange={e => setInput(e.target.value)}
              onKeyDown={e => { if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); send(); } }}
              placeholder="Type a question..."
              style={{
                flex: 1, background: 'rgba(255,255,255,0.05)', border: '1px solid rgba(124,189,212,0.15)',
                borderRadius: 8, padding: '8px 12px', color: '#DCE4EE', fontSize: 13,
                fontFamily: "'Sora', sans-serif", outline: 'none',
              }}
            />
            <button
              onClick={send}
              disabled={!input.trim() || loading}
              aria-label="Send message"
              style={{
                width: 36, height: 36, borderRadius: 8, border: 'none', cursor: 'pointer',
                background: input.trim() && !loading ? 'linear-gradient(135deg,#7CBDD4,#4890aa)' : 'rgba(124,189,212,0.2)',
                display: 'flex', alignItems: 'center', justifyContent: 'center', flexShrink: 0,
                transition: 'background 0.2s',
              }}
            >
              <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke={input.trim() && !loading ? '#0D1520' : '#4A6580'} strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round">
                <line x1="22" y1="2" x2="11" y2="13"/><polygon points="22 2 15 22 11 13 2 9 22 2"/>
              </svg>
            </button>
          </div>
        </div>
      )}

      {/* Floating bubble */}
      <button
        onClick={() => setOpen(o => !o)}
        aria-label={open ? 'Close chat' : 'Open chat'}
        style={{
          position: 'fixed', bottom: 24, right: 24,
          width: 52, height: 52, borderRadius: '50%', border: 'none', cursor: 'pointer',
          background: 'linear-gradient(135deg,#7CBDD4,#4890aa)',
          display: 'flex', alignItems: 'center', justifyContent: 'center',
          boxShadow: '0 4px 20px rgba(124,189,212,0.45)',
          zIndex: 10000,
          transition: 'transform 0.2s, box-shadow 0.2s',
        }}
        onMouseEnter={e => { (e.currentTarget as HTMLButtonElement).style.transform = 'scale(1.08)'; }}
        onMouseLeave={e => { (e.currentTarget as HTMLButtonElement).style.transform = 'scale(1)'; }}
      >
        {open ? (
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#0D1520" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round">
            <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
          </svg>
        ) : (
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#0D1520" strokeWidth="2.2" strokeLinecap="round" strokeLinejoin="round">
            <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/>
          </svg>
        )}
      </button>

      {/* Dot animation keyframes */}
      <style>{`
        @keyframes chat-dot {
          0%, 60%, 100% { opacity: 0.3; transform: translateY(0); }
          30% { opacity: 1; transform: translateY(-3px); }
        }
      `}</style>
    </>
  );
}
```

- [ ] **Step 2: Verify the file was created**

```bash
ls components/ChatWidget.tsx
```

Expected: `components/ChatWidget.tsx`

- [ ] **Step 3: Commit**

```bash
git add components/ChatWidget.tsx
git commit -m "feat: add ChatWidget component"
```

---

### Task 4: Add ChatWidget to the landing page

**Files:**
- Modify: `app/page.tsx` (last line only)

The landing page's `return` statement currently is:
```tsx
return <div style={{ fontFamily: "'Sora', sans-serif", minHeight: '100vh' }} dangerouslySetInnerHTML={{ __html: MARKUP }} />;
```

It needs to be wrapped in a fragment with `<ChatWidget />` added after.

- [ ] **Step 1: Open `app/page.tsx` and verify the current last line**

```bash
tail -5 app/page.tsx
```

Expected to see:
```
  return <div style={{ fontFamily: "'Sora', sans-serif", minHeight: '100vh' }} dangerouslySetInnerHTML={{ __html: MARKUP }} />;
}
```

- [ ] **Step 2: Add the import at the top of `app/page.tsx`**

Find the existing imports at the top of the file (around line 1-3). Add:

```tsx
import ChatWidget from '@/components/ChatWidget';
```

The file currently starts with:
```tsx
'use client';
import { useEffect } from 'react';
```

Change to:
```tsx
'use client';
import { useEffect } from 'react';
import ChatWidget from '@/components/ChatWidget';
```

- [ ] **Step 3: Update the return statement**

Find:
```tsx
  return <div style={{ fontFamily: "'Sora', sans-serif", minHeight: '100vh' }} dangerouslySetInnerHTML={{ __html: MARKUP }} />;
```

Replace with:
```tsx
  return (
    <>
      <div style={{ fontFamily: "'Sora', sans-serif", minHeight: '100vh' }} dangerouslySetInnerHTML={{ __html: MARKUP }} />
      <ChatWidget />
    </>
  );
```

- [ ] **Step 4: Run the dev server and manually verify**

```bash
npm run dev
```

Open http://localhost:3000. You should see:
- A teal circular chat button in the bottom-right corner
- Clicking it opens the chat panel with the welcome message
- Typing a question and pressing Enter sends it
- A typing indicator (3 bouncing dots) appears while waiting
- The AI responds with a GameOn-relevant answer
- Clicking × or the bubble again closes the panel

- [ ] **Step 5: Commit**

```bash
git add app/page.tsx
git commit -m "feat: add ChatWidget to landing page"
```

---

### Task 5: Add ANTHROPIC_API_KEY + build check + deploy

**Files:**
- Vercel environment variables (via dashboard or CLI)

- [ ] **Step 1: Add the API key to Vercel**

Go to https://vercel.com → GameOn project → Settings → Environment Variables.

Add:
- Key: `ANTHROPIC_API_KEY`
- Value: your Anthropic API key (get from https://console.anthropic.com)
- Environment: Production, Preview, Development

- [ ] **Step 2: Run a production build locally to catch TypeScript errors**

```bash
npm run build
```

Expected: `✓ Compiled successfully` with no errors. If there are TypeScript errors, fix them before continuing.

- [ ] **Step 3: Deploy to Vercel**

```bash
git push origin main
```

Vercel auto-deploys on push. Watch the deployment at https://vercel.com/signevallin/gameon.

- [ ] **Step 4: Verify on production**

Open https://playgameon.app. Confirm:
- Chat bubble visible bottom-right
- Opens, sends a message, gets a reply
- Typing indicator works
- Panel closes on × click
